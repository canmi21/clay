{
  "scripts": {
    "clean": "cargo clean",
    "publish": "cargo publish",
    "build": "cargo build",
    "add": "cargo add",
    "install": "cargo install --path .",
    "lint": "cargo fmt --all",
    "dev": "cargo run"
  }
}