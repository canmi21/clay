{
  "scripts": {
    "add": "cargo add",
    "remove": "cargo remove",
    "build": "cargo build",
    "publish": "cargo publish",
    "dev": "cargo run",
    "install": "cargo install --path .",
    "clean": "cargo clean",
    "lint": "cargo fmt --all"
  }
}